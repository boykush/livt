# The site renders this repository, and this repository's artifacts are
# Japanese — a reader of 実例マッピング boards should not have to read them
# through English furniture. livt's default is en; this is a per-repository
# choice, not a property of the tool (AGENTS.md, "Language").
lang: ja

{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "livt-serve",
      "runtimeExecutable": "zsh",
      "runtimeArgs": ["-lc", "go run . serve"],
      "port": 3000
    },
    {
      "name": "docs-site",
      "runtimeExecutable": "python3",
      "runtimeArgs": ["-c", "import functools, http.server as s\nclass NoStore(s.SimpleHTTPRequestHandler):\n    def end_headers(self):\n        self.send_header('Cache-Control', 'no-store')\n        super().end_headers()\ns.ThreadingHTTPServer(('', 3001), functools.partial(NoStore, directory='docs/site')).serve_forever()"],
      "port": 3001
    }
  ]
}
